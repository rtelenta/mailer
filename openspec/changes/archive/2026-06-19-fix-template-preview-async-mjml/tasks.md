## Tasks

- [x] In `features/templates/pages/TemplateEditorPage.tsx`, make `compilePreview` an `async` function and add `await` before the `mjml()` call
- [x] In the preview `useEffect` setTimeout callback, make it `async` and `await` the `compilePreview` call

## Tasks

- [x] In `bruno/send-email.bru`, remove `content` from the default JSON body so the minimal example omits it, and add a `//`-commented block showing how to include `content` for templates with Handlebars variables
